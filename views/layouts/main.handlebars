<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Wellness Wizard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #e4f4f2;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        .heading {
            background-color: #168cdb;
            color: whitesmoke;
            padding: 20px 0;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            box-shadow: 0px 4px 5px 0px rgba(0, 0, 0, 0.1);
            margin-top: -80px;
        }


        .container {
            max-width: 1000px;
        }

        /* Add a class to your <nav> element for better targeting */
        .navbar {
            position: fixed;
            /* Fixed position to stick it at the top */
            top: 0;
            /* Position it at the top of the page */
            left: 0;
            right: 0;
            /* Make it span the full width */
            background-color: #bdecf3;
            /* Change the background color to your preference */
            padding: 5px 0;
            /* Add padding to create space between the navbar and content */
            z-index: 1000;
            /* Ensure it's above other elements */
        }

        /* Style the links inside the navbar */
        .navbar-item {
            color: darkslategrey;
            /* Text color for links */
            margin: 0 15px;
            /* Add spacing between the links */
            text-decoration: none;
            /* Remove underline */
        }

        /* Style the section containing the header */
        .section {
            margin-top: 60px;
            /* Add margin to push content below the navbar */
            /* Add other styling as needed */
        }


        .header {
            background-color: #00D1B2;
            color: white;
            border-radius: 8px;
            padding: 10px;
            margin-bottom: 20px;
            text-align: center;
        }

        .columns {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
            margin: 20px 0;
            justify-content: space-around;
        }

        .column {
            flex: 1;
            min-width: 150px;
            box-sizing: border-box;
        }

        .card {
            flex: 0 0 30%;
            background-color: whitesmoke padding: 20px;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
        }

        .card:hover {
            transform: translateY(-10px);
        }

        /* Individual card styles */
        .user-info {
            border: 2px solid #96f463;
        }

        .magical-inspiration {
            border: 2px solid #ea912b;
        }

        .hydration-tracker {
            border: 2px solid #6ffbed;
        }

        .fitness-tracker {
            border: 2px solid #f2f45c;
        }

        .sleep-log {
            border: 2px solid #ce70f0;
        }

        .calorie-intake {
            border: 2px solid #f25850;
        }
    </style>
</head>

<body>
    <section class="section">
        <div class="container">
            {{{body}}}
        </div>
    </section>

</body>

</html>