<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Wellness Wizard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #e4f4f2;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

       .heading {
    background-color: #168cdb;
    color: #ffffff;
    padding: 20px 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    box-shadow: 0px 4px 5px 0px rgba(0, 0, 0, 0.1);
    margin-top: -80px;
}


        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        .columns {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
            margin: 20px 0;
            justify-content: space-around;
        }

        .column {
            flex: 1;
            min-width: 150px;
            box-sizing: border-box;
        }

        .card {
            box-shadow: 0px 2px 10px 0px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease-in-out;
            min-height: 200px;
            width: 100%;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
        }

        .card-content {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 1rem;
            cursor: pointer;
            color: #ffffff;
            text-align: center;
        }

        .user-info { background-color: #f1a9a0; }
        .magical-inspiration { background-color: #9b59b6; }
        .hydration-tracker { background-color: #6bb9f0; }
        .fitness-tracker { background-color: #1abc9c; }
        .sleep-log { background-color: #f39c12; }
        .calorie-intake { background-color: #e74c3c; }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none;
            z-index: 999;
        }

        .overlay.active { display: block; }

.icon-wrapper {
    background-color: inherit;
    border-radius: 50%;
    width: 60px; 
    height: 60px; 
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 10px;
}

.icon {
    font-size: 1.8rem;
    color: #ffffff;
}

        @media (max-width: 768px) {
            .column, .icon, .title {
                font-size: 1.2rem;
            }
        }

        @media (max-width: 576px) {
            .column, .icon, .title, .heading {
                font-size: 1rem;
            }
            .heading {
                padding: 1rem 0;
                line-height: normal;
            }
        }

        @media (max-width: 576px) {
            .column, .icon, .title, .heading {
                font-size: 0.8rem;
            }
            .heading {
                padding: 0.8rem 0;
                line-height: 1.2;
            }
            .card-content {
                padding: 0.5rem;
            }
            .icon-wrapper {
                padding: 5px;
            }
        }
    </style>
</head>

<body>
    <section class="section">
        <div class="container">
            {{{body}}}
        </div>
    </section>
</body>

</html>