<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
    <title>WellnessWizard</title>
    <style>
        body {
            border: 2px solid lightgray;
        }

        .container {
            max-width: 1000px;
        }

        .header {
            border: 2px solid #00D1B2;
            color: white;
            border-radius: 8px;
            padding: 10px;
            margin-bottom: 20px;
            text-align: center;
        }

        .row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .card {
            flex: 0 0 30%;
            border: 2px solid #e0e0e0;
            padding: 20px;
            box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
        }

        .card:hover {
            transform: translateY(-10px);
        }

    /* Individual card styles */
        .user-info {border: 3px solid #ffddc1;}
        .magical-inspiration {border: 3px solid #c2c2f0;}
        .hydration-tracker {border: 3px solid #c1e7e3;}
        .fitness-tracker {border: 3px solid #d5e8d4;}
        .sleep-log {border: 3px solid #fff2cc;}
        .calorie-intake {border: 3px solid #f8cecc;}
    </style>
</head>

<body>
    <section class="section">
        <div class="container">
            {{{body}}}
        </div>
    </section>
</body>

</html>